name: Compressed Size

on:
    pull_request:
    workflow_dispatch:

concurrency:
    group: ${{ github.workflow }}-${{ github.event_name == 'pull_request' && github.head_ref || github.sha }}
    cancel-in-progress: true

jobs:
    build:
        name: Building and Give Sizes
        runs-on: ubuntu-latest
        timeout-minutes: 30
        if: ${{ github.repository == 'blockeraai/blockera-site-toolkit' || github.event_name == 'pull_request' }}
        env:
            BLOCKERA_GLOBAL_PACKAGES_TOKEN: ${{ secrets.BLOCKERABOT_PAT }}
            BLOCKERA_BUNDLE_SIZE_PATHS_FILE: .github/bundle-size-paths
        steps:
            - uses: actions/checkout@v5
              with:
                  token: ${{ secrets.BLOCKERABOT_PAT }}
                  fetch-depth: 0

            - name: Ensure global-packages submodule
              uses: ./.github/actions/ensure-global-packages

            - uses: ./packages/global-packages/packages/dev-tools/github/actions/bundle-size
              with:
                  repo-token: ${{ secrets.BLOCKERABOT_PAT }}
                  pattern: '{dist/**/*.min.js,dist/**/*.css,blockera-site-toolkit.zip}'
